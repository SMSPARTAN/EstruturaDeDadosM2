#pragma once

#include "Node.hpp"

template <typename T>
class HuffmanTree {
private:
  Node<T> *root;

public:
  explicit HuffmanTree(T data) {
    root(data);
  }

  void insert(T data) {
    while(true) {
      
    }
  }
};